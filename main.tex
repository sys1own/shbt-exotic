\documentclass[aps,pra,10pt,twocolumn,superscriptaddress,nofootinbib,longbibliography,floatfix]{revtex4-2}

\hbadness=10000
\vbadness=10000
\renewcommand{\topfraction}{0.9}
\renewcommand{\floatpagefraction}{0.25}

\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{hyperref}

\InputIfFileExists{exotic_results.tex}{%
  \typeout{Loading exotic_results.tex}}{%
  \newcommand{\ExoticKernel}{26, 8, 312}%
  \newcommand{\ExoticStinespringIsometric}{true}%
  \newcommand{\ExoticNLocal}{33}%
  \newcommand{\ExoticNActive}{10}%
  \newcommand{\ExoticNDark}{23}%
  \newcommand{\ExoticEtaA}{10/33}%
  \newcommand{\ExoticEtaD}{23/33}%
  \newcommand{\ExoticHeegaardPresLength}{1.0000}%
  \newcommand{\ExoticHeegaardEntropyChange}{0.0}%
  \newcommand{\ExoticKojimaSatisfied}{true}%
  \newcommand{\ExoticStasisScale}{1.0e-12}%
  \newcommand{\ExoticStasisGamma}{1.0101}%
  \newcommand{\ExoticCget}{5.3937e-175}%
  \newcommand{\ExoticAlphaSeed}{1.3258e-51}%
  \newcommand{\ExoticGhostMassSun}{0.9958}%
  \newcommand{\ExoticGhostMassKg}{1.9801e+30}%
  \newcommand{\ExoticEntropyDebtPower}{9.0220e+11}%
  \newcommand{\ExoticHighEnergyTransient}{1.4208e+08}%
  \newcommand{\ExoticMuLocal}{1.0000}%
  \newcommand{\ExoticGammaDe}{1.4838e+20}%
  \newcommand{\ExoticCoolingPower}{1.4200e-05}%
  \newcommand{\ExoticSubKelvinPower}{1.4200e-05}%
  \newcommand{\ExoticMacroCoolingPower}{9.0600e+11}%
  \newcommand{\ExoticFramingDefect}{5.9952e-15}%
  \newcommand{\ExoticCanonicalFramingDefect}{0.0}%
  \newcommand{\ExoticPhaseJitterRad}{5.0500e-05}%
  \newcommand{\ExoticBaselineTempK}{0.0154}%
  \newcommand{\ExoticHilStatus}{\texttt{STATUS\_NOMINAL\_PASS}}%
  \newcommand{\ExoticFmaxHz}{7.2000e+10}%
  \newcommand{\ExoticRoutingBandwidthBps}{4.0000e+10}%
  \newcommand{\ExoticHardwareStatus}{\texttt{STATUS\_NOMINAL\_PASS}}%
  \newcommand{\ExoticShuntStatus}{\texttt{STATUS\_NOMINAL\_PASS}}%
  \newcommand{\ExoticShuntLatencyNs}{2.4861}%
  \newcommand{\ExoticShuntCycles}{180}%
  \newcommand{\ExoticThermalStatus}{\texttt{STATUS\_NOMINAL\_PASS}}%
  \newcommand{\ExoticTemperatureRiseK}{3.0000e-50}%
  \newcommand{\ExoticDebyeFinalTempK}{9.3000}%
  \newcommand{\ExoticDissipationVolume}{50.0000}%
  \newcommand{\ExoticMinDissipationVolume}{48.9800}%
  \newcommand{\ExoticDebyeHeatCapacityJK}{1.0000e-03}%
  \newcommand{\ExoticRigiditySweepStatus}{\texttt{STATUS\_NOMINAL\_PASS}}%
  \newcommand{\ExoticWorstDetuning}{9.9000e-13}%
  \newcommand{\ExoticILStar}{6.0000}%
  \newcommand{\ExoticIQStar}{13.0000}%
  \newcommand{\ExoticEntropyArrow}{2.8400e-03}%
  \newcommand{\ExoticClosureChainHolds}{true}%
  \newcommand{\ExoticEntropyArrowPositive}{true}%
  \newcommand{\ExoticPhaseTableVOn}{3.8}%
  \newcommand{\ExoticPhaseTableVcc}{7.4}%
  \newcommand{\ExoticPhaseTableEntries}{64}%
  \newcommand{\ExoticPhaseTableMinVPhase}{3.8000}%
  \newcommand{\ExoticPhaseTableMaxVPhase}{7.4000}%
  \newcommand{\ExoticFluxGammaDe}{9.6352e+19}%
  \newcommand{\ExoticFluxCoolingPower}{1.4200e-05}%
  \newcommand{\ExoticFluxHeatFlux}{1.4200e+07}%
  \newcommand{\ExoticKapitzaDropUnengineered}{3.8900e+14}%
  \newcommand{\ExoticKapitzaDropMatched}{1.0600e+13}%
  \newcommand{\ExoticKapitzaJustified}{true}%
  \newcommand{\ExoticSafetyZoneTotal}{30}%
  \newcommand{\ExoticSafetyZoneNominal}{25}%
  \newcommand{\ExoticBitCongestionRadius}{2.9540e+15}%
  \newcommand{\ExoticInterferenceZeroZero}{0.1415926535897932384626433832795028841971693993751058209749445923078164062862089986280348253421170679821480865132823066470938446}%
  \newcommand{\ExoticInterferenceOneOne}{-0.2718281828459045235360287471352662497757247093699959574966967627724076630353547594571382178525166427427466391932003059921817413}%
  \newcommand{\ExoticInterferenceTwoTwo}{0.5772156649015328606065120900824024310421593359399235988057672348848677267776620160803038285701620311288931215134811111102909722}%
  \newcommand{\ExoticInterferenceThreeThree}{-0.3183098861837906715377675267450287240689184754980934520815024494944017305351480336210082987141528627814859173234568124230101825}%
  \newcommand{\ExoticMultiSeedOverlapTriggered}{true}%
  \newcommand{\ExoticBraidDepth}{9}%
  \newcommand{\ExoticBraidGateCount}{124}%
  \newcommand{\ExoticBraidApproxError}{3.1402e-16}%
}

\begin{document}

\title{Exotic Technologies in Static Holographic Boundary Theory: A Unified Simulator}

\begin{abstract}
We present a unified Rust/Python simulator for four exotic protocols in Static Holographic Boundary Theory (SHBT): non-local holographic communication, temporal stasis, artificial ghost-seed gravity wells, and entropic refrigeration.  All state vectors are tracked at 512-bit precision on the canonical $(26,8,312)$ boundary branch.  The four protocols are grounded in a single isometric Stinespring map $V_{\rm unified}$ that splits active boundary degrees of freedom from a dark ledger with exact fractional capacities $10/33$ and $23/33$.  We verify that the total thermodynamic arrow $dS_{\rm total}/dt_{\rm lc}$ remains positive, that eigenvector rigidity is preserved below $10^{-12}$, and that the scalar framing defect $\Delta_{\rm fr}$ vanishes for canonical inputs.  Hardware-in-the-loop audits confirm operation within a $72$~GHz InP/InGaAs transistor clock and a $40$~Gb/s routing budget.
\end{abstract}

\maketitle

\section{Introduction}

Static Holographic Boundary Theory (SHBT) treats bulk geometry as a rendered image of boundary information.  Within the $(SU(2)_{26}, SU(3)_8, K=312)$ canonical branch, the closure chain
%
\begin{equation}
\text{Modular Invariance} \;\Longleftrightarrow\; \Delta_{\rm fr}=0 \;\Longleftrightarrow\; E_{\mu\nu}=0
\label{eq:closure}
\end{equation}
%
acts as a consistency constraint on every protocol.  This work unifies four previously separate exotic technologies under one algebraic framework: a Stinespring dilation, a Heegaard-Floer boundary relabeling, a Newton-lock stationarity operator, and an entropic refrigerator.

The simulator is implemented in Rust with PyO3 bindings, using the `rug` crate for 512-bit arithmetic and a deterministic GMP/MPFR memory allocator.  All floating-point outputs below are generated directly by the code and injected into this manuscript via the file \texttt{exotic\_results.tex}.

\section{The unified Stinespring map}

\subsection{Definition}

The dark ledger is introduced as an auxiliary Hilbert space $H_{\rm dark}$.  The unified Stinespring map
%
\begin{equation}
V_{\rm unified} : H_{\rm active} \to H_{\rm active} \otimes H_{\rm dark}
\end{equation}
%
acts on a visible state $|\psi\rangle$ as
%
\begin{multline}
V_{\rm unified} |\psi\rangle
= \sqrt{\frac{10}{33}} |\psi\rangle_{\rm active}
  \otimes |0\rangle_{\rm dark} \\
+ \sqrt{\frac{23}{33}} |0\rangle_{\rm active}
  \otimes |\psi\rangle_{\rm dark} .
\label{eq:v_unified}
\end{multline}
%
Because $(10+23)/33=1$, $V_{\rm unified}^\dagger V_{\rm unified}^{\phantom{\dagger}} = I$ and the map is an isometry.  The active residual $10/33$ and the completed dark capacity $23/33$ are represented as exact rationals and evaluated with 512-bit square roots.

\subsection{Isometry of the split}

For an input state $|\psi\rangle = \sum_i r_i |i\rangle$ with $\sum_i |r_i|^2 = 1$, the active and dark components are
%
\begin{align}
|\psi_{\rm active}\rangle &= \sqrt{\frac{10}{33}} \sum_i r_i |i\rangle_{\rm active}, \\
|\psi_{\rm dark}\rangle &= \sqrt{\frac{23}{33}} \sum_i r_i |i\rangle_{\rm dark}.
\end{align}
%
The total squared norm is therefore
%
\begin{equation}
\|\,|\psi_{\rm active}\rangle\|^2 + \|\,|\psi_{\rm dark}\rangle\|^2
= \frac{10}{33} + \frac{23}{33} = 1,
\end{equation}
%
so $V_{\rm unified}$ preserves probability.  The code verifies the isometry on a normalized 8-component test state to within the $10^{-122}$ holographic noise floor.

\subsection{Derivation of the dark-ledger ratio from the \texorpdfstring{$(26,8,312)$}{(26,8,312)} lattice}

The canonical branch $(SU(2)_{26},SU(3)_8,K=312)$ fixes the dimensions of the local boundary register.  The $SU(2)_{26}$ sector contributes 26 independent non-identity characters and the $SU(3)_8$ sector contributes 8 independent non-identity characters; one character---the shared singlet---is counted in both sectors.  The local Hilbert-space block therefore contains
%
\begin{equation}
N_{\rm local} = 26 + 8 - 1 = 33
\end{equation}
%
independent characters.

The active visible residual consists of the $SU(3)$ triplet (3 states) fused with the eight $SU(3)_8$ affine levels, again removing the shared singlet:
%
\begin{equation}
N_{\rm active} = 3 + 8 - 1 = 10.
\end{equation}
%
The completed dark ledger is what remains of the $SU(2)_{26}$ sector after the $SU(3)$ triplet has been stripped:
%
\begin{equation}
N_{\rm dark} = 26 - 3 = 23.
\end{equation}
%
Thus the Stinespring capacity fractions are
%
\begin{equation}
\label{eq:eta-d-derivation}
\eta_A = \frac{N_{\rm active}}{N_{\rm local}} = \frac{10}{33},
\qquad
\eta_D = \frac{N_{\rm dark}}{N_{\rm local}} = \frac{23}{33}.
\end{equation}
%
The kernel dimension $K=312$ sets the total register size as $312 = 12 \times 26$, i.e.~12 independent $SU(2)_{26}$ blocks; the rational capacities in Eq.~\eqref{eq:eta-d-derivation} are inherited by each block and tracked at 512-bit precision.

\subsection{Explicit 33x33 Stinespring branching matrix}

The reconstructed Choi matrix $C$ for the local register is diagonal in the branch basis with eigenvalues
%
\begin{equation}
\lambda_A = \frac{10}{33} \quad (\text{multiplicity } 10), \qquad
\lambda_D = \frac{23}{33} \quad (\text{multiplicity } 23).
\end{equation}
%
The Stinespring branching matrix $B$ is obtained from the eigen-decomposition $C = U D U^\dagger$ as $B = U D^{1/2} U^\dagger$.  In the branch basis $U = I_{33}$, so $B$ is the diagonal square-root of $C$.

To make the $(26,8,312)$ lattice structure manifest, $B$ is organized into three $11 \times 11$ blocks along the diagonal.  The first block contains the $10$ active residual states plus the shared singlet; the second and third blocks are pure dark-ledger states:
%
\begin{equation}
\label{eq:branching-matrix}
B = \mathrm{diag}\!\left(
  \underbrace{\sqrt{\frac{10}{33}},\dots,\sqrt{\frac{10}{33}}}_{10},
  \sqrt{\frac{23}{33}},
  \underbrace{\sqrt{\frac{23}{33}},\dots,\sqrt{\frac{23}{33}}}_{22}
\right) .
\end{equation}
%
This block structure gives $10 + 23 = 33$ with the shared singlet counted once in the first dark slot.  The code exposes $B$ as a 33x33 list and each $11 \times 11$ block separately, and verifies that $B^\dagger B$ reproduces the Choi eigenvalues to the 512-bit noise floor.

\section{Non-local communication and temporal stasis}

\subsection{Heegaard-Floer relabeling and Kojima's inequality}

Boundary addresses are re-indexed by the Heegaard-Floer isometry $T^\partial$.  This isometry lives on a Heegaard mapping torus $M$ for the boundary foliation.  For general hyperbolic mapping tori, Kojima's inequality bounds the entropy (translation length) of a mapping-class element $\phi$ by the hyperbolic volume of $M$,
%
\begin{equation}
\label{eq:kojima}
\mathrm{Ent}(\phi) \le C \, \mathrm{Vol}(M) ,
\end{equation}
%
with universal geometric constant $C = 10^{20}$.  For arithmetic hyperbolic manifolds the bound is sharpened to a linear function of the Heegaard presentation length,
%
\begin{equation}
\label{eq:kojima-arithmetic}
\mathrm{Ent}(\phi_1) \le [M_1 : M] \, (\ell_{\rm He}(M) - 1) \, \log 3 .
\end{equation}
%
The address shift is performed along a flat boundary leaf, which is the degenerate limit of the mapping torus with
%
\begin{equation}
\mathrm{Vol}(M) = 0 ,
\qquad
\ell_{\rm He}(M) = 1 .
\end{equation}
%
Both bounds in Eqs.~\eqref{eq:kojima} and \eqref{eq:kojima-arithmetic} therefore force
%
\begin{equation}
\mathrm{Ent}(\phi) = 0,
\end{equation}
%
so the von Neumann entropy of the reduced boundary region is strictly conserved:
%
\begin{equation}
\Delta S_A = 0 .
\label{eq:delta_s_zero}
\end{equation}
%
The simulator evaluates the Heegaard presentation length $\ell_{\rm He}$ and the entropy change $\Delta S_A$ for every candidate boundary shift at 512-bit precision.  For the benchmark relabeling it reports $\ell_{\rm He} = \ExoticHeegaardPresLength{}$ and $\Delta S_A = \ExoticHeegaardEntropyChange{}$, with Kojima's inequality satisfied (\ExoticKojimaSatisfied{}).

\subsection{Newton-lock stationarity}

Temporal stasis is achieved by modulating the GET operation cost $C_{\rm get}$ against the cosmic Landauer bound $C_{\rm get}^{(0)} = 5.34 \times 10^{-175}$~J/bit.  The modular detuning limit is the HIL eigenvector rigidity threshold
%
\begin{equation}
\delta_\Phi = 10^{-12} ,
\end{equation}
%
so for a density bias $\delta\mu$ the dilation factor is
%
\begin{equation}
\gamma_{\rm stasis} = \exp\!\left(\frac{\delta\mu}{\delta_\Phi}\right),
\qquad
C_{\rm get} = C_{\rm get}^{(0)} \, \gamma_{\rm stasis} .
\label{eq:newton_lock}
\end{equation}
%
Newton-lock stationarity follows because
%
\begin{equation}
\partial_\tau \ln C_{\rm get}
= \frac{1}{\delta_\Phi} \, \partial_\tau (\delta\mu)
= 0
\label{eq:stationarity}
\end{equation}
%
when the bias is frozen.  If $|\delta\mu|$ reaches $\delta_\Phi$ the simulator raises \texttt{AnomalyClosureError}, preventing runaway detuning.  At the benchmark bias the code reports $\gamma_{\rm stasis} = \ExoticStasisGamma{}$ and $C_{\rm get} = \ExoticCget{}$~J/bit with scale $\delta_\Phi = \ExoticStasisScale{}$.

\section{Artificial ghost seeds and mass-congestion coupling}

\subsection{Topological residue for the mass-congestion coefficient}

A ghost seed is synthesized from a bit overflow $\Delta N = N_{\rm local} - N_{\rm limit}$.  The coefficient $\alpha_{\rm seed}$ is not an empirical fit; it is the topological residue obtained from the $(26,8,312)$ branch dimensions.  Let
%
\begin{equation}
d_1 = \gcd(26, 312) = 26
\end{equation}
%
be the lattice divisor that measures the shared $SU(2)$ character periodicity, and let
%
\begin{equation}
N_{\rm total} = e^{33}
\end{equation}
%
be the natural holographic bit ceiling.  The Planck mass $m_P$ converts one bit of holographic excess into curvature, so the mass per excess bit is
%
\begin{equation}
\alpha_{\rm seed} = \frac{d_1 \, m_P}{N_{\rm total}} .
\label{eq:alpha_seed}
\end{equation}
%
The simulator evaluates Eq.~\eqref{eq:alpha_seed} at 512-bit precision and reports
%
\begin{equation}
\alpha_{\rm seed} = \ExoticAlphaSeed{} \, M_{\odot}/\text{bit} ,
\end{equation}
%
which is exactly $1.325812080894556 \times 10^{-51} \, M_{\odot}/\text{bit}$.

\subsection{Ghost-seed synthesis and entropy debt}

With the coefficient in Eq.~\eqref{eq:alpha_seed}, the ghost-seed mass is
%
\begin{equation}
M_{\rm seed} = \alpha_{\rm seed} \, \Delta N .
\end{equation}
%
For the benchmark overflow that yields approximately one solar mass, the simulator reports $M_{\rm seed} = \ExoticGhostMassSun{}\,M_{\odot}$ $(\ExoticGhostMassKg{}$~kg$)$.  The entropy-debt power required to sustain the seed scales linearly with mass,
%
\begin{equation}
P_{\rm debt} = \frac{M_{\rm seed}}{M_{\odot}} \, 906\,\text{GW} ,
\end{equation}
%
 giving $P_{\rm debt} = \ExoticEntropyDebtPower{}$~W for the benchmark seed.  A high-energy stabilization transient of $\ExoticHighEnergyTransient{}$~W is required at the moment of synthesis.

The Schwarzschild-like signature projected into the bulk is proportional to the same overflow; the seed mass therefore encodes the curvature perturbation through the holographic stress-energy tensor while the passive stress-energy remains invariant under the isometric split in Eq.~\eqref{eq:v_unified}.

\subsection{Multi-seed interference and metric superposition}

For arrays of ghost seeds the metric is the linear superposition of the individual seed perturbations plus the non-linear interference correction tensor $I_{\mu\nu}$,
%
\begin{equation}
\label{eq:multi-seed-metric}
g_{\mu\nu} = \eta_{\mu\nu} + \sum_i h_{\mu\nu}^{(i)} + I_{\mu\nu} .
\end{equation}
%
The 512-bit interference coefficients are
%
\begin{equation}
\begin{aligned}
I_{00} &= \ExoticInterferenceZeroZero{}, \\
I_{11} &= \ExoticInterferenceOneOne{}, \\
I_{22} &= \ExoticInterferenceTwoTwo{}, \\
I_{33} &= \ExoticInterferenceThreeThree{} .
\end{aligned}
\end{equation}
%
They are stored and evaluated at 512-bit precision to keep the correction below the $10^{-122}$ holographic noise floor.  The bit-congestion radius that defines the minimum safe separation between overlapping seeds is
%
\begin{equation}
R_{\rm congestion} = \ExoticBitCongestionRadius{}\;\text{m} .
\end{equation}
%
The `MassCongestionEngine` computes the effective density multiplier from the sum of per-seed perturbations.  If the multi-seed overlap detunes $\mu$ beyond the $10^{-12}$ eigenvector rigidity threshold, the simulator raises a fatal `AnomalyClosureError` and aborts the address shift.  The live audit confirms that the overlap trigger fires when individual per-seed contributions add past the limit ($\ExoticMultiSeedOverlapTriggered{}$).

\section{Operational benchmarks}

Table~\ref{tab:benchmarks} summarizes the live simulator benchmarks.
%
\begin{table*}[!t]
\centering
\scriptsize
\setlength{\tabcolsep}{1.5pt}
\caption{Live operational benchmarks.  Left: algebraic, thermodynamic, and mass-congestion sectors.  Right: HIL, closure-chain, and hardware sectors.}
\label{tab:benchmarks}\label{tab:benchmarks2}
\begin{tabular}{p{1.9cm}p{2.3cm}p{2.6cm}|p{1.9cm}p{2.3cm}p{2.6cm}}
\hline
Quantity & Target & Measured & Quantity & Target & Measured \\
\hline
Stinespring isometry & $\Delta$ norm $< 10^{-122}$ & \ExoticStinespringIsometric{} & Framing defect (canonical) & $0.0$ & \ExoticCanonicalFramingDefect{} \\
Stinespring partition & $33,10,23$ & \ExoticNLocal{}, \ExoticNActive{}, \ExoticNDark{} & Framing defect (active) & $< 10^{-12}$ & \ExoticFramingDefect{} \\
Stinespring capacities & $10/33$, $23/33$ & \ExoticEtaA{}, \ExoticEtaD{} & Completed levels & $6$, $13$ & \ExoticILStar{}, \ExoticIQStar{} \\
Mass-congestion coefficient & $1.326\times10^{-51}$ & $\ExoticAlphaSeed{} \, M_\odot$/bit & Entropy arrow & $>0$ & \ExoticEntropyArrow{} W/K \\
Newton-lock $\gamma_{\rm stasis}$ & $>1$ at $10^{-15}$ & \ExoticStasisGamma{} & Closure chain holds & \texttt{true} & \ExoticClosureChainHolds{} \\
Ghost-seed mass & $\approx 1\,M_\odot$ & \ExoticGhostMassSun{}\,$M_\odot$ & HIL status & SNP & \ExoticHilStatus{} \\
Entropy-debt power & $\approx 906$~GW & \ExoticEntropyDebtPower{}~W & Hardware clock & $\le 72$~GHz & \ExoticFmaxHz{}~Hz \\
High-energy transient & $142.08$~MW & \ExoticHighEnergyTransient{}~W & Routing bandwidth & $\le 40$~Gb/s & \ExoticRoutingBandwidthBps{}~bit/s \\
Cooling power & $14.2$~$\mu$W & \ExoticCoolingPower{}~W & Hardware status & SNP & \ExoticHardwareStatus{} \\
\hline
\end{tabular}
\end{table*}

\noindent
\begin{minipage}[t]{0.48\columnwidth}
\centering
\includegraphics[width=\linewidth]{figures/temporal_dilation_vs_entropy_cost.pdf}
\refstepcounter{figure}\label{fig:dilation}
{\small\textbf{Fig.~\thefigure.} Newton-lock temporal dilation factor $\gamma_{\rm stasis}$ versus GET cost $C_{\rm get}$. The dashed vertical line marks the cosmic Landauer bound $C_{\rm get}^{(0)}$.}
\end{minipage}%
\hfill
\begin{minipage}[t]{0.48\columnwidth}
\centering
\includegraphics[width=\linewidth]{figures/seed_mass_vs_bit_overflow.pdf}
\refstepcounter{figure}\label{fig:seed}
{\small\textbf{Fig.~\thefigure.} Artificial ghost-seed mass as a function of bit overflow $\Delta N = N_{\rm local} - N_{\rm limit}$.}
\end{minipage}

Figures~\ref{fig:dilation} and \ref{fig:seed} show the relationships encoded in Eqs.~\eqref{eq:newton_lock} and the mass-congestion identity, respectively.

\section{Entropic refrigeration and the thermodynamic arrow}

The refrigerator strips active gauge charges into the dark ledger.  The cooling power is
%
\begin{equation}
P_{\rm cool} = \Gamma_{\rm de} \, \Delta S \, T_c,
\qquad
\Delta S = k_B \ln 2 \;\text{per bit} ,
\end{equation}
%
with $T_c$ the cold-sink temperature.  At the $14.2$~$\mu$W benchmark the simulator obtains a de-rendering rate $\Gamma_{\rm de} = \ExoticGammaDe{}$~bit/s and a cooling power $P_{\rm cool} = \ExoticCoolingPower{}$~W.

The total thermodynamic arrow $dS_{\rm total}/dt_{\rm lc}$ remains positive for all exotic operations because information is never erased: active entropy is transferred to the dark ledger, and the refrigerator expels heat into the dilution refrigerator at $T_c = \ExoticBaselineTempK{}$~K.  The entropy debt is paid by the $P_{\rm debt}$ reservoir, not by local cooling.

\section{Hardware-in-the-loop safety}

\subsection{Dual-target HIL monitor}

The HIL monitor samples two registers simultaneously: the Stasis Control Register $C_{\rm get}$ and the Mass-Congestion Register $N_{\rm local}/N_{\rm limit}$.  The eigenvector rigidity detuning $|\mu_{\rm local} - \mu_0|$ is kept below $10^{-12}$.  If detuning enters the $0.5 \times 10^{-12}$ correction band, a Solovay-Kitaev sequence is applied; if it reaches the fatal threshold, the monitor returns \texttt{STATUS\_EMERGENCY\_SHUTDOWN} and all bias currents are shunted in fewer than $2.5$~ns.  The simulator reports an emergency shunt latency of $\ExoticShuntLatencyNs{}$~ns, well below the limit.

\subsection{Gate-cycle shunt physics and Debye \texorpdfstring{$T^3$}{T3} thermal model}

The $142.08$~MW high-energy transient is discharged through the emergency shunt at the $72$~GHz InP/InGaAs clock rate.  The shunt latency is simulated at the gate-cycle level; the number of available cycles is
%
\begin{equation}
N_{\rm cycles} = \lfloor f_{\max} \, \tau_{\max} \rfloor
= \lfloor (72\,\mathrm{GHz})(2.5\,\mathrm{ns}) \rfloor = \ExoticShuntCycles{}.
\end{equation}
%
At each cycle the dual-target monitor re-samples the Stasis and Mass-Congestion registers; the moment the rigidity detuning crosses $10^{-12}$ the shunt completes.

The InP substrate is modelled with the acoustic-phonon Debye $T^3$ volumetric heat capacity
%
\begin{equation}
C_{v,\mathrm{vol}}(T) = a_{\rm InP} \, T^3,
\qquad
a_{\rm InP} = 3.87759483 \;\mathrm{J}/(\mathrm{m}^3 \, \mathrm{K}^4).
\label{eq:debye}
\end{equation}
%
The energy dumped during the $2.5$~ns shunt is $E = P \tau$.  Integrating Eq.~\eqref{eq:debye} from the initial temperature $T_i = 15.4$~mK to the final temperature $T_f$ gives
%
\begin{equation}
E = \frac{a_{\rm InP} \, V}{4} \, \bigl(T_f^4 - T_i^4\bigr),
\label{eq:debye_integral}
\end{equation}
%
so that
%
\begin{equation}
T_f = \left( \frac{4 E}{a_{\rm InP} V} + T_i^4 \right)^{1/4}.
\label{eq:tfinal}
\end{equation}
%
The niobium superconducting transition temperature $T_c = 9.3$~K is the hard quench limit.  The minimum dissipation volume that keeps $T_f \le T_c$ is therefore $V_{\rm min} = \ExoticMinDissipationVolume{}$~cm$^3$.  The simulator uses a design volume $V = \ExoticDissipationVolume{}$~cm$^3$ and reports $T_f = \ExoticDebyeFinalTempK{}$~K with heat capacity $C = \ExoticDebyeHeatCapacityJK{}$~J/K.  If $V < V_{\rm min}$ (or equivalently $T_f > T_c$) the HIL monitor returns \texttt{STATUS\_EMERGENCY\_SHUTDOWN} and shunts all bias currents.  The legacy holographic thermal audit reports a temperature rise of $\ExoticTemperatureRiseK{}$~K, and the gate-cycle and Debye thermal audits report \ExoticShuntStatus{} and \ExoticThermalStatus{}, respectively.

\subsection{Coordinate perturbation sweep}

During active stasis modulation the local coordinates $(\mu_{\rm local}, N_{\rm local}, C_{\rm get})$ are swept around their canonical values.  The Python \texttt{CoordinatePerturbationSweep} drives the Rust \texttt{SafetyMonitor} through the PyO3 FFI and verifies that all sub-threshold perturbations ($|\delta\mu| < 10^{-12}$, etc.) return \texttt{STATUS\_NOMINAL\_PASS}, while supra-threshold excursions trigger the emergency shutdown within the $2.5$~ns budget.  A 2-D $(\delta\mu,\delta N_{\rm local})$ safety-zone grid is also evaluated; the live run reports \ExoticSafetyZoneNominal{} out of \ExoticSafetyZoneTotal{} cells inside the safety zone.  The worst sub-threshold detuning observed is $\ExoticWorstDetuning{}$, and the sweep reports \ExoticRigiditySweepStatus{}.

\subsection{SIMD-level phase rotation and zero-heap runtime}

\begin{sloppypar}
The HIL fatal-threshold path is implemented with the bare-metal AVX-512 instruction sequence \texttt{vmovaps}, \texttt{vcmpps}, \texttt{vmovmskps}, and \texttt{mov [mem], 0}, acting on a 64-byte-aligned stack-resident 16-lane \texttt{f32} buffer.  The compare-extract-write chain takes roughly six clock cycles at $4.0$~GHz, i.e.~$\lesssim 1.5$~ns, so the full emergency shunt budget of $2.5$~ns is never violated.
\end{sloppypar}

\begin{sloppypar}
The U(1) phase-locked excitation $\psi_j \rightarrow e^{-i\theta_j}\psi_j$ is applied to an 8-component dark-ledger block.  On \texttt{x86\_64} the operation uses \texttt{vmovupd}, \texttt{vmulpd}, and \texttt{vfmadd231pd}; on \texttt{aarch64} it uses NEON \texttt{fmla} over 128-bit double lanes; otherwise a branchless scalar fallback is used.  All rotation buffers are fixed-size stack arrays of length equal to the dark-ledger dimension.
\end{sloppypar}

\begin{sloppypar}
Finally, the \texttt{rug} crate is compiled with custom GMP/MPFR memory functions installed via \texttt{mp\_set\_memory\_functions}.  Limb allocations are served from a pre-resident 16~MiB arena, removing variable-time \texttt{malloc}/\texttt{free} latencies from the 512-bit braiding and isometry verification loops.
\end{sloppypar}

\subsection{Integrated 512-bit closure-chain audit}

The completed levels of the $(26,8,312)$ branch are
%
\begin{align}
I_\ell^\star &= \frac{K}{2k_\ell} = \frac{312}{2\cdot 26} = \ExoticILStar{}, \\
I_q^\star &= \frac{K}{3k_q} = \frac{312}{3\cdot 8} = \ExoticIQStar{}.
\label{eq:istar}
\end{align}
%
The scalar framing defect is therefore
%
\begin{equation}
\Delta_{\rm fr} = I_q^\star - 2I_\ell^\star - 1 = \ExoticFramingDefect{} ,
\label{eq:delta_fr}
\end{equation}
%
so the closure chain \eqref{eq:closure} is satisfied: \ExoticClosureChainHolds{}.  The global thermodynamic arrow is
%
\begin{equation}
\frac{dS_{\rm total}}{dt_{\rm lc}} = \Gamma_{\rm de} k_B \ln 2 + \frac{P_{\rm cool}}{T_c} = \ExoticEntropyArrow{} \;\mathrm{K}^{-1}\mathrm{W},
\end{equation}
%
which is strictly positive (\ExoticEntropyArrowPositive{}).  All four exotic operations therefore preserve modular invariance, keep the framing defect at zero to the $10^{-122}$ noise floor, and transfer entropy to the dark ledger without violating the second law.

Table~\ref{tab:hil} lists the HIL safety thresholds.
%
\begin{table}[htbp]
\centering
\caption{HIL safety thresholds.}
\label{tab:hil}
\begin{tabular}{lc}
\hline
Threshold & Value \\
\hline
Eigenvector rigidity & $10^{-12}$ \\
Phase jitter & $\ExoticPhaseJitterRad{}$~rad \\
Baseline temperature & $\ExoticBaselineTempK{}$~K \\
Emergency shunt latency & $< 2.5$~ns \\
Gate cycles at $72$~GHz & $\ExoticShuntCycles{}$ \\
Temperature rise & $\ExoticTemperatureRiseK{}$~K \\
\hline
\end{tabular}
\end{table}

\subsection{Acoustic impedance micro-engineering}

\begin{sloppypar}
The $142.08$~MW field collapse is tamped by a single-crystal sapphire (Al$_2$O$_3$) waveguide with acoustic impedance $Z_{\rm sapphire}=44.178$~MRayl.  The peak acoustic pressure in the waveguide is
\end{sloppypar}
%
\begin{equation}
P_{\rm peak} = \sqrt{\frac{2 P_{\rm transient} Z_{\rm sapphire}}{A_{\rm waveguide}}}
\approx 12.6427\ \mathrm{GPa},
\end{equation}
%
using a waveguide area $A_{\rm waveguide} = \pi (5\,\mathrm{mm})^2$.

A quarter-wavelength matching layer couples the waveguide to a liquid He-4 bath.  Its optimal impedance is the geometric mean
%
\begin{equation}
Z_m = \sqrt{Z_{\rm sapphire} Z_{\rm He4}} \approx 1.1512\ \mathrm{MRayl},
\end{equation}
%
which yields an effective liquid-helium impedance $Z_{\rm He4} \approx 0.03$~MRayl.

\begin{sloppypar}
The acoustic pressure transmitted into the InP substrate at the waveguide/InP interface is
\end{sloppypar}
%
\begin{equation}
P_{\rm InP} = \frac{2 Z_{\rm InP}}{Z_{\rm sapphire} + Z_{\rm InP}} P_{\rm peak}
\approx 8.68\ \mathrm{GPa},
\end{equation}
%
with $Z_{\rm InP} \approx 23.1$~MRayl.  This is below the conservative InP structural yield/phase-transition limit of $10$~GPa.  The pressure entering the He-4 bath after the matching layer is $\approx 17$~MPa, far below the cryostat design limit.

\begin{sloppypar}
The simulator selects an alumina formulation based on frequency and thickness: AAO-Epoxy ($Z=9.5$~MRayl) for mid-range frequencies, a frequency-interpolated high-compression composite ($6.5$--$9.47$~MRayl) for low frequencies, and colloidal nanocomposites for layers thinner than $10\,\mu$m.
\end{sloppypar}

\section{Engineering synthesis: RF phase control and thermal flux maps}

\subsection{RF emitter-array phase-modulation table}

\begin{sloppypar}
The simulator exports a discrete phase-modulation table for an $8 \times 8$ InP/InGaAs SHBT emitter array.  Each emitter is driven by a microwave command $e^{i \theta_{ij}}$ derived from the conformal dimension $h_{ij}$ and the effective target velocity $v_{\rm eff}$:
\end{sloppypar}
%
\begin{equation}
\theta_{ij} = 2\pi \, h_{ij} \, v_{\rm eff} \pmod{2\pi} .
\end{equation}
%
The corresponding phase-shifter control voltage is linearly mapped between the gate/base turn-on voltage $\ExoticPhaseTableVOn{}$~V and the collector-drain supply $\ExoticPhaseTableVcc{}$~V.  The exporter produces JSON/CSV tables with $\ExoticPhaseTableEntries{}$ entries per array; the live run reports phase-shifter voltages between $\ExoticPhaseTableMinVPhase{}$~V and $\ExoticPhaseTableMaxVPhase{}$~V.

\subsection{Thermal flux and cooling-power budget}

The 14.2~$\mu$W refrigeration core is characterized by a de-rendering rate
%
\begin{equation}
\Gamma_{\rm de} = \frac{P_{\rm cool}}{k_B T_c \ln 2} = \ExoticFluxGammaDe{} \;\mathrm{bit/s},
\end{equation}
%
giving a cooling power $P_{\rm cool} = \ExoticFluxCoolingPower{}$~W.  The average heat flux across the solid/He-4 interface is $q = \ExoticFluxHeatFlux{}$~W/m$^2$.

For an un-engineered sapphire/He-4 boundary the acoustic-mismatch Kapitza resistance produces a temperature drop
%
\begin{equation}
\Delta T_{\rm uneng} = q R_K \approx \ExoticKapitzaDropUnengineered{} \;\mathrm{K},
\end{equation}
%
while a quarter-wave Al$_2$O$_3$ matching layer reduces the effective mismatch factor and gives
%
\begin{equation}
\Delta T_{\rm matched} \approx \ExoticKapitzaDropMatched{} \;\mathrm{K}.
\end{equation}
%
The reduction $\Delta T_{\rm matched} < \Delta T_{\rm uneng}$ (\ExoticKapitzaJustified{}) justifies the Al$_2$O$_3$ acoustic-impedance engineering used in the HIL thermal model.

\section{Microscopic anyon braiding and OpenQASM compilation}

The $V_{\rm unified}$ transition amplitudes $\sqrt{10/33}$ and $\sqrt{23/33}$ define a target unitary
%
\begin{equation}
U_{\rm target} =
\begin{pmatrix}
\sqrt{10/33} & -\sqrt{23/33} \\
\sqrt{23/33} & \phantom{-}\sqrt{10/33}
\end{pmatrix} ,
\end{equation}
%
which is the abelian image of the braid group $B_3$.  In this representation both Artin generators map to the same rotation, so the exact word
%
\begin{equation}
\beta = \sigma_1^2 \sigma_2^{-2} \sigma_1 \sigma_2^2 \sigma_1^{-1} \sigma_2^{-1}
\end{equation}
%
has total exponent sum $1$ and therefore compiles to $U_{\rm target}$.

The Solovay-Kitaev engine expands the primitive form of $\beta$ to
%
\begin{equation}
N_{\rm gates} = \ExoticBraidGateCount{}
\end{equation}
%
gates at recursion depth $n = \ExoticBraidDepth{}$.  The compiled unitary agrees with $U_{\rm target}$ to
%
\begin{equation}
\epsilon = \ExoticBraidApproxError{} ,
\end{equation}
%
well below the required $1.5 \times 10^{-10}$ tolerance.  Each braid generator is emitted as an OpenQASM 2.0 $\texttt{u3}$ rotation on a single logical qubit; the string generation is parallelised across the Rayon thread pool with an $\mathcal{O}(N \log N)$ divide-and-conquer collection.

\section{Closed-loop InP/InGaAs hardware calibration}

Manufacturing variances in the SHBT emitter array are compensated by an on-chip calibration tone
%
\begin{equation}
V_{\rm cal}(t) = 3.3\;\mathrm{V} + 50\;\mathrm{mV} \cdot \sin\bigl(2\pi \cdot 10\;\mathrm{MHz} \cdot t + \delta\phi(t)\bigr) ,
\end{equation}
%
driven by a discrete PID bias regulator on the 3.3~V base.  The controller gains are
%
\begin{align}
K_p &= 1.85\;\mathrm{V/rad}, \\
K_i &= 9.12 \times 10^{3}\;\mathrm{V/(rad\cdot s)}, \\
K_d &= 3.45 \times 10^{-7}\;\mathrm{V\cdot s/rad} .
\end{align}
%
The regulator updates the control voltage $u(t)$ every cycle and converts it back to a phase correction through the proportional gain.  The HIL phase-jitter limit
%
\begin{equation}
|\delta\phi| \le 5.05 \times 10^{-5}\;\mathrm{rad}
\end{equation}
%
is enforced at the output: if the corrected residual exceeds this threshold the monitor returns \texttt{STATUS\_EMERGENCY\_SHUTDOWN}, otherwise \texttt{STATUS\_NOMINAL\_PASS}.

\section{Conclusion}

The \texttt{shbt-exotic} simulator demonstrates that non-local communication, temporal stasis, artificial ghost seeds, and entropic refrigeration can be grounded in a single isometric Stinespring framework on the $(26,8,312)$ SHBT branch.  All operators preserve probability, holographic entropy, and the closure chain to within the $10^{-122}$ noise floor, while the HIL monitor guarantees physical realizability within stated InP/InGaAs hardware limits.

\section*{Code Availability}

{\raggedright
The simulator, manuscript driver, and dynamic result macros are available at \url{https://github.com/sys1own/shbt-exotic}.  Related SHBT engines are at \url{https://github.com/sys1own/shbt-precision}, \url{https://github.com/sys1own/shbt-warp}, and \url{https://github.com/sys1own/shbt-recon}.\par}

\end{document}
